#!/bin/bash

# Split the PATH variable using ":" as the delimiter and store the result in an array
IFS=':' read -ra directories <<< "$PATH"

# Count the number of elements in the array
count=${#directories[@]}

# Print the count
echo "$count"

